# Docker Compose port overrides for local dev.
# Copy: cp docker-compose.dev.env.example docker-compose.dev.env
# Use:  docker compose --env-file docker-compose.dev.env -f docker-compose.dev.yml up -d
#
# Adjust if ports conflict on your machine.

POSTGRES_PORT=5433
REDIS_PORT=6381
